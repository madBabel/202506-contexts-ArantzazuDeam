name: Contexts
on:
    push:
    workflow_dispatch:
        inputs:
            debug:
                description: 'Debug mode'
                type: boolean
                default: false

jobs:
    echo-data:
        runs-on: ubuntu-latest
        steps: 
            - name: Show Info
              run: | 
                echo "Nombre del evento: ${{github.event_name}}"
                echo "Ref: ${{github.ref}}"
            - name: Retrieve Variable
              run: |
                echo "Mi variable de entorno: $MY_VAR"
                echo "Mi variable de repositorio $ORG_VAR"
                